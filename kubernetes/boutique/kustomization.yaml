apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: boutique

resources:
  - namespace.yaml
  - base

patches:
  - path: overlays/dev/redis-cart-resources.yaml
    target:
      group: apps
      version: v1
      kind: Deployment
      name: redis-cart
